name: flutter_icons_null_safety
description: Local wrapper for flutter_font_icons to avoid breaking changes.
version: 1.1.0
publish_to: "none"

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_font_icons: ^2.2.7
